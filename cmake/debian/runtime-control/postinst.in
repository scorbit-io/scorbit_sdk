#!/bin/sh
set -e
CONF=/etc/ld.so.conf.d/scorbit-sdk.conf
if [ "$1" = "configure" ]; then
  mkdir -p /etc/ld.so.conf.d
  echo "@SCORBIT_SDK_LD_SO_PATH@" > "$CONF"
  if [ -d "@SCORBIT_SDK_INSTALL_PREFIX@" ]; then
    chmod 1777 "@SCORBIT_SDK_INSTALL_PREFIX@" || true
  fi
fi
ldconfig
